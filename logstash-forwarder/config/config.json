{
  "network": {
    "servers": [ "logstash-sb:5043" ],
    "timeout": 15
  },
  "files": [
    {
      "paths": [
        "/var/log/syslog",
        "/var/log/auth.log"
      ],
      "fields": { "type": "syslog" }
    }
  ]
}
